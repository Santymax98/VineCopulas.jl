import{_ as i,o as a,c as s,ao as n}from"./chunks/framework.D3aZiJV4.js";const u=JSON.parse('{"title":"Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"developer/architecture.md","filePath":"developer/architecture.md","lastUpdated":null}'),t={name:"developer/architecture.md"};function l(o,e,p,r,c,h){return a(),s("div",null,[...e[0]||(e[0]=[n(`<h1 id="Architecture" tabindex="-1">Architecture <a class="header-anchor" href="#Architecture" aria-label="Permalink to &quot;Architecture {#Architecture}&quot;">​</a></h1><p><code>VineCopulas.jl</code> is organized around three layers: pair-copula primitives, vine traversal, and fitting/selection. The package is the native Julia vine layer on top of <code>Copulas.jl</code>; it should not duplicate <code>Copulas.jl</code> as a general pair-copula mathematics library.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/</span></span>
<span class="line"><span>├─ PairCopulas/          conditional kernels and pair log densities</span></span>
<span class="line"><span>├─ Vines/                C-, D-, and R-vine structures and traversal</span></span>
<span class="line"><span>├─ fitting.jl            shared fitting utilities and entry point</span></span>
<span class="line"><span>├─ fitting/</span></span>
<span class="line"><span>│  ├─ pairs.jl           pair fitting, rotations, selection domains</span></span>
<span class="line"><span>│  ├─ cd_vines.jl        C- and D-vine sequential fitting</span></span>
<span class="line"><span>│  └─ rvines.jl          R-vine graph, structure selection, fitting</span></span>
<span class="line"><span>├─ VineCopula.jl         common vine interface and conditional API</span></span>
<span class="line"><span>├─ stats.jl              log-likelihood, parameter counts, AIC, BIC</span></span>
<span class="line"><span>└─ utils.jl              shared numerical utilities</span></span></code></pre></div><p>The fitting layer is deliberately split into only three implementation files. The goal is to separate responsibilities without turning every helper into its own module.</p><h2 id="Pair-layer" tabindex="-1">Pair layer <a class="header-anchor" href="#Pair-layer" aria-label="Permalink to &quot;Pair layer {#Pair-layer}&quot;">​</a></h2><p>Pair copulas come from <code>Copulas.jl</code>. In particular, <code>PairCopula</code> is an alias for <code>Copulas.Copula{2}</code> rather than a VineCopulas-specific family hierarchy.</p><p>The semantic fallback for pair conditionals is the public <code>Copulas.jl</code> conditioning interface:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cdf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">condition</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(C, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, v), u)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cdf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">condition</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(C, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, u), v)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">quantile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">condition</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(C, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, v), q)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">quantile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">condition</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(C, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, u), q)</span></span></code></pre></div><p><code>hfunc1</code>, <code>hfunc2</code>, <code>hinv1</code>, and <code>hinv2</code> are public vine terminology for these operations. A compatible bivariate <code>Copulas.jl</code> copula should therefore be usable on a vine edge without any VineCopulas-specific methods.</p><p><code>VineCopulas.jl</code> may still add fast or numerically stable implementations of <code>hfunc1</code>, <code>hfunc2</code>, <code>hinv1</code>, <code>hinv2</code>, and pair log-density hooks where the generic fallback is insufficient or materially slower. These specializations are optional fast paths, not a second mathematical source of truth.</p><p>Hot vine traversals use a small internal fused protocol:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">_pair_step</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(C, u, v, buf)       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># (logpdf, h1, h2)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">_pair_logpdf_h1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(C, u, v, buf)  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># (logpdf, h1)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">_pair_logpdf_h2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(C, u, v, buf)  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># (logpdf, h2)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">_pair_hfuncs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(C, u, v)          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># (h1, h2)</span></span></code></pre></div><p>The generic methods compose the standalone primitives, so arbitrary compatible bivariate copulas retain the same evaluation contract. Family specializations reuse transformed coordinates. In particular, Gaussian and Student fused steps compute the two marginal quantiles once per edge/observation instead of once per primitive. Archimedean specializations dispatch through generator-level hooks so family-specific stability formulas are preserved. These functions are internal; the public h-function API is unchanged.</p><p>Batched internal helpers (<code>_pair_hfuncs!</code>, <code>_pair_hfunc1!</code>, <code>_pair_hfunc2!</code>) support local scratch reuse during fitting and traversal without storing mutable workspaces in a vine object.</p><p>Reusable mathematical behavior for an individual pair-copula family should generally live in <code>Copulas.jl</code>. Fused operations that exist because a vine traversal needs several quantities at once belong in <code>VineCopulas.jl</code>.</p><h2 id="Vine-layer" tabindex="-1">Vine layer <a class="header-anchor" href="#Vine-layer" aria-label="Permalink to &quot;Vine layer {#Vine-layer}&quot;">​</a></h2><p><code>CVineCopula</code>, <code>DVineCopula</code>, and <code>RVineCopula</code> own structure validation and traversal. Evaluation code does not perform model selection. Density traversal requests only the fused subset required by the next tree: C-vines propagate one conditional, D-vines propagate both, and the standard R-vine execution plan uses per-edge liveness. The final active tree computes density only because no later pair consumes its conditional outputs.</p><p>Work buffers are owned by each call. D-vine density updates disjoint active states in place rather than copying the complete left/right matrices at every tree; Rosenblatt and inverse Rosenblatt reuse call-local work arrays. This keeps the engines reentrant and thread-safe while reducing allocation pressure.</p><h2 id="Fitting-layer" tabindex="-1">Fitting layer <a class="header-anchor" href="#Fitting-layer" aria-label="Permalink to &quot;Fitting layer {#Fitting-layer}&quot;">​</a></h2><p>Fitting reuses the public vine types. Sequential estimation creates the same model objects that users can construct explicitly. Selection-specific parameter bounds live only in the fitting layer and do not modify the underlying <code>Copulas.jl</code> family definitions.</p><p>Quick fitting, for example <code>fit(RVineCopula, U)</code>, returns the fitted vine copula itself. Rich fitting metadata belongs in <code>CopulaModel</code>, not in the vine distribution object. This keeps the probabilistic model distinct from diagnostics such as selection scores, fitting method, convergence status, selected truncation, and selection traces.</p>`,21)])])}const k=i(t,[["render",l]]);export{u as __pageData,k as default};
